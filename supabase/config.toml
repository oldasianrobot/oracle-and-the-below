[functions.oracle-dialogue]
# The function validates the user's JWT using auth.getUser before any work.
verify_jwt = false

[functions.oracle-access]
# Code sessions are verified on the server; instructor actions separately require Auth.getUser.
verify_jwt = false
